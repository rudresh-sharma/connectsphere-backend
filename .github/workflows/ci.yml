name: Backend CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  backend:
    name: Backend build and tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Test backend
        run: mvn test

  deploy-aws:
    name: Deploy backend to AWS
    runs-on: ubuntu-latest
    needs: backend
    if: >-
      github.event_name != 'pull_request' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') &&
      secrets.AWS_EC2_HOST != '' &&
      secrets.AWS_EC2_USER != '' &&
      secrets.AWS_EC2_SSH_KEY != ''
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          BRANCH_NAME: ${{ github.ref_name }}
          BACKEND_APP_DIR: ${{ vars.BACKEND_APP_DIR }}
          BACKEND_COMPOSE_FILE: ${{ vars.BACKEND_COMPOSE_FILE }}
        with:
          host: ${{ secrets.AWS_EC2_HOST }}
          username: ${{ secrets.AWS_EC2_USER }}
          key: ${{ secrets.AWS_EC2_SSH_KEY }}
          envs: BRANCH_NAME,BACKEND_APP_DIR,BACKEND_COMPOSE_FILE
          script: |
            set -eu

            APP_DIR="${BACKEND_APP_DIR:-$HOME/connectsphere-backend}"
            COMPOSE_FILE="${BACKEND_COMPOSE_FILE:-docker-compose.app.yml}"

            cd "$APP_DIR"
            git fetch origin "$BRANCH_NAME"
            git checkout "$BRANCH_NAME"
            git reset --hard "origin/$BRANCH_NAME"
            docker compose -f "$COMPOSE_FILE" up -d --build
