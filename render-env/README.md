Render env templates for backend services.

Use Render's `Add from .env` option and paste the contents of the service file you need.

Files currently included:

- `eureka-server.env.example`
- `auth-service.env.example`

Replace every `your-...` placeholder before deploying.
